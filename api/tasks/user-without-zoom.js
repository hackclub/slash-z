// if we find a host in airtable without a zoom account, invite them
// if we find a host in airtable without a zoom ID, record their zoom ID

const { default: fetch } = require("node-fetch")

module.exports = async (req, res) => {
}